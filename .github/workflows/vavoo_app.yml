Name: vavoo_app
on:
  schedule:
    - cron: "0 */18 * * *"
  workflow_dispatch:

jobs:
  fetch_m3u8_job:
    runs-on: ubuntu-latest
    permissions: # Yazma izni verildiğinden emin oluyoruz
      contents: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: pip install curl_cffi requests

      - name: Run Python Script
        run: |
          # Scripti indir ve çalıştır
          curl -L -o vavoo_app.py "${{ secrets.VAVOO_APP_URL }}"
          python vavoo_app.py
        env:
          # Eğer scriptin içinde GitHub token kullanıyorsan buraya ekle
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Commit and push changes
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          
          # Dosyanın oluşup oluşmadığını kontrol et (Hata almamak için)
          if [ -f vavoo_app.m3u8 ]; then
            git add vavoo_app.m3u8
            # Sadece değişiklik varsa commit yap
            git diff --staged --quiet || (git commit -m "🔄 Otomatik Güncelleme: $(date +'%Y-%m-%d %H:%M')" && git push)
          else
            echo "⚠️ HATA: vavoo_app.m3u8 dosyası oluşturulamadı!"
            exit 1
          fi
